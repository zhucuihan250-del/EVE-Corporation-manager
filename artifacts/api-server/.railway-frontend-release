# Railway currently watches this service directory for deploy triggers.
# Update this marker when a frontend-only release must rebuild the API service,
# which also bundles and serves the production frontend.
game-style-system-map-2026-09-12
